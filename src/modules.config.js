// THE ONLY core file touched to add, remove or reorder minigames.
// Play order is shuffled per run (registry.runOrder); this list only says which games are on. Ids must match src/modules/<id>/index.js.
export default [
  'lucky-dip', // risk appetite (Mia)
  'torch-talk', // effective communication (Noah)
  // 'sample-tap' retired 2026-09-26 (test module; folder kept as a coding example)
];
